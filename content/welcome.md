# Welcome

This is a bare markdown file at the root of `content/`. It has no frontmatter, so it dispatches to the default type — **note**.

Try editing the title and body, then save. The file should round-trip back to disk with frontmatter populated.

You can also create wikilinks like [[standup]] and tags like #demo.
